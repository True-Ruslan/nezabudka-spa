{
  "name": "nezabudka-spa",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "verify:build": "node scripts/verify-static-build.mjs",
    "preview": "astro preview",
    "audit:links": "node scripts/check-external-links.mjs",
    "audit:lighthouse": "npm exec --yes --package=@lhci/cli@0.15.1 -- lhci autorun --config=./lighthouserc.cjs",
    "test:quality": "node --test tests/external-links.test.mjs tests/axe-config.test.mjs tests/lighthouse-config.test.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.2"
  }
}
